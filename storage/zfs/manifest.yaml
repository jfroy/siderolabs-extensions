version: v1alpha1
metadata:
  name: zfs
  version: "$VERSION"
  author: Andrei Kvapil, Enix
  description: |
    This system extension provides the ZFS kernel module, the ZFS utilities, and a service to import all ZFS pools.
  compatibility:
    talos:
      version: ">= v1.6.0"
